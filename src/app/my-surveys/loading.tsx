// Route-segment loading fallback for the surveyor's "My Surveys" screen.
import LoadingScreen from "@/src/components/LoadingScreen";

export default function Loading() {
  return <LoadingScreen label="Loading your surveys…" />;
}
