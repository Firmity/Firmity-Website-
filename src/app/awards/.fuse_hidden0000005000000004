import type { ReactNode } from "react"
import { noindexMetadata } from "@/src/lib/seo"

export const metadata = noindexMetadata("Awards")

export default function Layout({ children }: { children: ReactNode }) {
  return children
}
